#include <stdio.h>
#include <math.h>

int main()
{
    int a,b;
    scanf("%d,%d",&a,&b);
    int c,d;
    scanf("%d,%d",&c,&d);
    int e,f;
    scanf("%d,%d",&e,&f);
    double di;
    di=(1/2)*((a* abs(d-f))+(b* abs(c-e))+ abs(c-(e*d)));
    printf("%lf",di);
    return 0;
}
