#include <stdio.h>
int Power(int n){
    int c=0;
    while(n && n&(n-1)){
        n=n&(n-1);
    }
    return n;
}
int main() {
    int n;
    scanf("%d",&n);
    printf("%d %d",Power(n));   
    return 0;
}
