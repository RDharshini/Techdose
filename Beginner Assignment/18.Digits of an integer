#include <stdio.h>
#include <math.h>
int main()
{
    long long int a;
    scanf("%lld",&a);
    int arr[1000],in=0;
    while(a>0){
        arr[in++]=a%10;
        a=a/10;
    }
    for(int i=in-1;i>=0;i--){
        printf("%d,",arr[i]);
    }
    return 0;
}
