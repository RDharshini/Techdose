#include <stdio.h>
#include <math.h>
int main() {
    int a,b;
    scanf("%d,%d",&a,&b);
    int c,d;
    scanf("%d,%d",&c,&d);
    int e,f;
    scanf("%d,%d",&e,&f);
    double area;
    area=(0.5)*(abs(a*(d-f) + c*(f-b) + e*(b-d)));
    printf("%lf",area);
    return 0;
}
