class Solution {
    public int maxProductDifference(int[] nums) {
        Arrays.sort(nums);
        int a=0;
        for(int i=0;i<nums.length;i++){
            a=nums[nums.length-1]*nums[nums.length-2] - nums[0]*nums[1];
    
        }
        return a;
    }
}
